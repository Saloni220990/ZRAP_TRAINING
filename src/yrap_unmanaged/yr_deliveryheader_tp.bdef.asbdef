unmanaged implementation in class zbp_r_deliveryheader_tp unique;
strict ( 2 );

define behavior for YR_DELIVERYHEADER_TP alias DelHead
//late numbering
lock master
authorization master ( instance )
//etag master <field_name>
{
  create ( authorization : global );
  update;
  delete;
  field ( readonly : update ) Vbeln;
  association _item { create; }

  mapping for yylikp
    {
      Vbeln = vbeln ;
    }
}

define behavior for YI_DELIVERYITEM alias DelLine
//late numbering
lock dependent by _header
authorization dependent by _header
//etag master <field_name>
{
  update;
  delete;
  field ( readonly ) Vbeln;
  field ( readonly : update ) Posnr;
  association _header;
}